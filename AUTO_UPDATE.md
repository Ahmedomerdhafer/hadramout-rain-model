# التحديث التلقائي

## ما تم تفعيله

توجد جدولة خارجية مرتبطة بالمشروع تعمل تلقائياً كل ست ساعات، وتنفّذ `update_all.sh` على نسخة حديثة من مستودع GitHub ثم تدفع النتائج إلى الفرع `main`. عند التشغيل، يبحث النظام عن أحدث دورة مكتملة ومتاحة معاً من GFS وAI-GFS خلال آخر 72 ساعة، ثم ينفّذ الخطوات التالية:

1. تنزيل البيانات الجزئية المطلوبة من مصادر NOAA العامة.
2. بناء سيناريوهات 24 ساعة و10 أيام للخطين GFS وAI-GFS.
3. توليد الخرائط الثمانية والتقارير المصاحبة.
4. تشغيل فحص الرصد في محطة OYRN.
5. إعادة بناء `index.html` و`preview.html`.
6. دفع النتائج الجديدة تلقائياً إلى الفرع `main`.

يمكن تشغيل العملية محلياً من مجلد المشروع بالأمر:

```bash
bash update_all.sh
```

ولا يحتاج المستخدم إلى تعديل تواريخ اللوحات يدوياً؛ إذ يتولى `update_all.py` مزامنة أسماء ملفات النتائج وعناوين النوافذ والدورة المختارة قبل الرسم.

## معنى ذلك عند فتح الرابط

الرابط المعروض من GitHub أو GitHub Pages هو موقع ملفات ثابتة؛ لذلك يعرض **آخر نسخة رفعتها عملية التحديث**. ستصل النسخة الجديدة تلقائياً بعد أقرب تشغيل ناجح، بحد أقصى تقريبي قدره ست ساعات، ولا يلزم إبقاء المتصفح مفتوحاً.

أما تحديث البيانات في نفس الثانية التي يفتح فيها كل زائر الرابط، فيحتاج إلى خادم خلفي دائم ينفذ التنزيل والرسم عند الطلب أو يعرض أحدث نسخة مخزنة. لا يمكن تنفيذ ذلك مباشرة من صفحة GitHub ثابتة بأمان، لأن تشغيل workflow عند كل زيارة يتطلب رمز وصول GitHub مكشوفاً في المتصفح ويعرّض المستودع للخطر.

| الخيار | المزايا | القيود | التكلفة/التعقيد |
|---|---|---|---|
| **التحديث الحالي كل 6 ساعات** | آمن، يعمل دون إبقاء المتصفح مفتوحاً، والنتائج محفوظة في GitHub | قد يتأخر التحديث حتى ست ساعات عن أحدث دورة | منخفض؛ مفعّل تلقائياً |
| **خادم خلفي دائم عند فتح الرابط** | يستطيع فحص أحدث دورة عند كل زيارة وعرض آخر نتيجة فوراً | يحتاج استضافة دائمة، وإدارة ذاكرة/تنزيلات، وحماية endpoint؛ الرسم قد يستغرق دقائق | أعلى؛ يحتاج نشر خدمة خلفية |
| **زر تحديث يدوي داخل الموقع** | تحديث فوري عند طلب المالك دون كشف رمز GitHub للزوار | ليس تحديثاً تلقائياً لكل زيارة، ويتطلب صلاحية مالك محمية | متوسط؛ يمكن إضافته لاحقاً |

الخيار المفعّل حالياً هو الأول لأنه يحقق التحديث الدوري الآمن ويحافظ على استقرار الرابط، وقد تم تسجيل جدولة المهمة في حساب المشروع. إذا كان المطلوب إلزامياً هو التحديث عند كل زيارة، فيلزم الانتقال إلى الخيار الثاني بخدمة خلفية دائمة، وليس مجرد صفحة GitHub ثابتة.
